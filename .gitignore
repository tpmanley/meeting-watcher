# Secrets — never commit your real OAuth client ID
Meeting Watcher/Secrets.xcconfig

# Internal distribution runbook — not meant for the public repo
HOMEBREW.md

# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
.build/

# macOS
.DS_Store

# Build output (regenerated by scripts/build-release.sh)
dist/*.zip

# Claude Code session state
.claude/settings.local.json
.claude/.cc-writes/
